<!DOCTYPE html>
<html lang="pt-br">
<head>
    <title>Projeto Integrador</title>
    <%- include('../partials/head.ejs'); -%>
    <link rel="stylesheet" type="text/css" href="css/home.css">
</head>
  <body>
    <div class="estrutura">
      <header class="header">
        <h1>Controle de gastos</h1>
      </header>
      <main class="content">
        <form action="/dadosUser" method="POST" class="formulario">
          <h2>Projeto integrador</h2>
          <div class="informacoes">
            <span>User: 
              <% if(typeof dadosUsuarios != 'undefined'){ %> 
                <%= dadosUsuarios.nome;  %>
              <% } %>
            </span><br>
            <span>id:
              <% if(typeof dadosUsuarios != 'undefined'){ %> 
                <%= dadosUsuarios.id;  %>
              <% } %>
            </span><br>
            <span>R$: 
              <% if(typeof dadosUsuarios != 'undefined'){ %> 
                <%= dadosUsuarios.total;  %>
              <% } %>
            </span>
          </div>
          <div class="caixa__receitas">
            <span>Receitas </span>
            <label for="valor__receita">Valor </label>
            <input type="number" name="valorReceita" min="0" id="valor__receita">
            <button type="submit" class="btn btn-primary">Enviar</button><br>
            <label for="categoria__receita">Categoria </label>
            <input type="text" name="categoriaReceita" class="categoria__receita" id="categoria__receita">
            <button type="submit" class="btn btn-primary">Enviar</button>
          </div>
          <div class="caixa__dispesas">
            <span>Dispesas</span>
            <label for="dispesas__valor">Valor </label>
            <input type="number" name="valorDispesa" min="0" class="dispesas__valor" id="dispesas__valor">
            <button type="submit" class="btn btn-primary">Enviar</button><br>
            <label for="dispesas__categoria">Categoria </label>
            <input type="text" name="categoriaDispesa" min="0" class="dispesas__categoria" id="dispesas__categoria">
            <button type="submit" class="btn btn-primary">Enviar</button><br>
            <a href="/logout" class="btn btn-link" id="sair">Sair</a>
            <a href="/historico" class="btn btn-link">Historico</a>
          </div>
        </form>
      </main>

      <footer class="footer">
        <p>&copy; Riam stefeson e Andre brendo</p>
      </footer>
    </div>
</body>
</html>